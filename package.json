{
  "name": "bun_datalibrary_ts",
  "module": "src/rdlib_cfsWorkflow.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@refinitiv-data/data": "^3.13.1-beta"
  },
  "scripts": {
    "build": "bun build ./src/rdlib_cfsWorkflow.ts --outdir ./dist --target node --minify",
    "compile": "bun build ./src/rdlib_cfsWorkflow.ts --compile --outfile ./dist/rdlib_cfsWorkflow",
    "start": "bun run build && bun run ./dist/rdlib_cfsWorkflow.js"
  }
}